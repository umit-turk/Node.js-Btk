<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/css/style.css">
  </head>
  <body>
   <%- include('../../views/partials/_nav.ejs') -%>

    <div class="container pt-100" >
      <div class="row">

       

 
        <div class="col-lg-12">

          <% if(action !== 'undefined' && action === 'delete') { %>
            <div class="alert alert-danger">The product has been deleted</div>
            <% } %>

          <% if(action !== 'undefined' && action === 'edit') { %>
            <div class="alert alert-success">The product has been updated</div>
            <% } %>

            <% if(products.length > 0) { %>
                <table class="table table-bordered">
                    <thead>
                        <th>Image</th>
                        <th>Product</th>
                        <th>Price</th>
                        <th>Image</th>
                    </thead>
                    <% for(let i =0; i < products.length; i++) { %>
                        <tr>
                            <td>
                                <img src="/img/<%= products[i].imageUrl %>" width="80px" alt="">
                            </td>
                            <td>
                                <%= products[i].name %>
                            </td>
                            <td>
                                <%= products[i].price %> TL
                            </td>
                            <td>
                                <a class="btn btn-primary btn-sm" href="/admin/products/<%= products[i].id %>">Edit</a>
                                <form action="/admin/delete-product" class="d-inline" method="POST">
                                  <input type="hidden" name="productId" value="<%= products[i].id %>">
                                  <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                                </form>
                            </td>
                        </tr>
                    <% } %>

                    <% if(products.length > 0) { %>
                        <h1>No products</h1>
                    <% } %>
                </table>
    
            <% } %>

        




        </div>
      </div>
    </div>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa"
      crossorigin="anonymous"
    ></script>
    <script src="https://kit.fontawesome.com/1c101fa5da.js" crossorigin="anonymous"></script>
  </body>
</html>
