<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title %></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <%- include('../../views/partials/_nav.ejs') -%>

    <div class="container pt-100">
      <div class="row">
        <div class="col-lg-3">
          <div class="list-group">
            <% if(categories.length> 0) { %> <% for(let i = 0; i <
            categories.length; i++) { %>
            <a
              href="/categories/<%= categories[i].id %>"
              class="list-group-item <%= categories[i].id === 'selectedCategory' ? 'active' : 'list-group-item' %>"
              ><%= categories[i].name %></a
            >
            <% } %> <% } %>
          </div>
        </div>
        <div class="col-lg-9">
          <div class="row">
            <% if(products.length > 0) { %> <% for(let i = 0; i <
            products.length; i++) { %>
            <div class="col-md-4">
              <div class="card">
                <img
                  src="/img/<%= products[i].imageUrl %>"
                  alt=""
                  class="card-img-top"
                />
                <div class="card-body">
                  <h4 class="card-title">
                    <a href="/products/<%=products[i].id %>"
                      ><%= products[i].name %></a
                    >
                  </h4>
                  <small class="text-muted">
                    <i class="fa fa-try"></i>
                    <%= products[i].price %>
                  </small>
                </div>
                <div class="card-footer text-center">
                  <a
                    href="/products/<%=products[i].id %>"
                    class="btn btn-secondary btn-sm"
                  >
                    Details
                  </a>
                  <a href="#" class="btn btn-secondary btn-sm ml-3">
                    <i class="fa fa-angle-double-right"></i>
                    Add to cart
                  </a>
                </div>
              </div>
            </div>
            <% } %> <% } %> <% if(products.length === 0) { %>
            <h1>Product yok</h1>
            <% } %>
          </div>
        </div>
      </div>
    </div>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa"
      crossorigin="anonymous"
    ></script>
    <script
      src="https://kit.fontawesome.com/1c101fa5da.js"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
